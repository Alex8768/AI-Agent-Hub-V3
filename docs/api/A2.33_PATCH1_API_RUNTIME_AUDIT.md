# A2.33 Patch 1 - API Runtime Audit

## Scope

Audit only. No API behavior changes in this patch.

Validated:

- documented endpoint inventory vs runtime router wiring;
- feature-flag gating on API routes;
- default runtime posture for flags affecting API availability.

## Runtime Endpoint Inventory (Current)

From `src/api/main.py` + endpoint routers:

- Health:
  - `GET /health`
  - `GET /api/v1/health`
  - `GET /api/v1/health/deep`
- LLM:
  - `POST /api/v1/llm/generate`
  - `POST /api/v1/llm/generate-stream`
- Documents:
  - `POST /api/v1/documents/upload`
  - `GET /api/v1/documents`
  - `GET /api/v1/documents/{document_id}`
  - `DELETE /api/v1/documents/{document_id}`
- Search:
  - `POST /api/v1/search`
  - `POST /api/v1/search-hybrid`
- Reasoning:
  - `POST /api/v1/answer`
- Tools:
  - `GET /api/v1/tools`
  - `GET /api/v1/tools/{tool_name}`
  - `POST /api/v1/tools/{tool_name}/invoke`
- Export:
  - `POST /api/v1/export`
  - `GET /api/v1/export/{export_id}/download`
- Streaming:
  - `GET /api/v1/stream/{session_id}`
- Diagnostics:
  - `GET /api/v1/trace-test`

## Feature-Flag Gating (Current Runtime)

- `POST /api/v1/answer` requires:
  - `feature_reasoning_api=True`
  - `feature_reasoning=True`
  - `feature_graphrag=True`
- `POST /api/v1/search-hybrid` requires:
  - `feature_hybrid_search_api=True`
  - `feature_graphrag=True`
- `/api/v1/tools*` requires:
  - `feature_reasoning_api=True`
- `GET /api/v1/stream/{session_id}` requires:
  - `streaming_enabled=True` (not `feature_*`, but runtime gate)

## Relevant Defaults (from `src/core/config.py`)

Default-off (affecting API access):

- `feature_reasoning_api=False`
- `feature_reasoning=False`
- `feature_graphrag=False`
- `feature_hybrid_search_api=False`
- `streaming_enabled=False`
- `rate_limit_enabled=False`

Implication: in default config, several advanced endpoints are registered but
return gated responses (typically 404 for feature-gated routes, 400 for streaming disabled).

## Drift Found vs `docs/api/README.md`

1. Missing documented endpoints:
   - `GET /api/v1/health/deep`
   - `DELETE /api/v1/documents/{document_id}`
   - `POST /api/v1/search-hybrid`
   - `POST /api/v1/answer`
   - `/api/v1/tools*`
   - `GET /api/v1/export/{export_id}/download`
   - `GET /api/v1/trace-test`
2. Documents path parameter mismatch in docs (`{id}` vs runtime `{document_id}`).
3. Docs do not describe runtime feature-gate behavior for advanced endpoints.
4. Rate limiting docs present as unconditional, but runtime default is disabled.

## Patch 2 Input

Use this audit as source for:

- endpoint list parity update;
- gating behavior note per endpoint group;
- corrected path parameter naming;
- explicit default-off statement for gated surfaces.
